# user config for sway
#
# Read `man 5 sway` for a complete reference.

include ~/.config/sway/config.d/*.conf

#system configs
include /etc/sway/config.d/*

#Exec kwallet to make it work
exec --no-startup-id /usr/libexec/pam_kwallet_init
